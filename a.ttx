falar pra insalar pyenv (3.11.9)

sudo apt update && sudo apt upgrade
sudo apt install -y \
  build-essential \
  libssl-dev \
  zlib1g-dev \
  libbz2-dev \
  libreadline-dev \
  libsqlite3-dev \
  curl \
  libncursesw5-dev \
  xz-utils \
  tk-dev \
  libxml2-dev \
  libxmlsec1-dev \
  libffi-dev \
  liblzma-dev \
  wget \
  git

ativar predictable network
(raspi config -> advanced -> network interfaces names)

sudo ~/.pyenv/versions/3.11.9/bin/python -m raspberryPI-FRC-config-tool.setup --type total
/home/frc9485/.pyenv/versions/3.11.9/bin/

sudo systemctl enable pigpiod
sudo systemctl start pigpiod

falar pra colocar no cmainho VisionScripts/vision.py
falar pra editar e fazer o própio arquivo SH (VisionScripts/start.sh)
